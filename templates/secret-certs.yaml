{{- if and .Values.security.enabled (eq .Values.security.certificates.strategy "auto") }}
# Empty secret placeholder - certificates will be generated by init container
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "nifi-registry.fullname" . }}-certs
  labels:
    {{- include "nifi-registry.labels" . | nindent 4 }}
type: Opaque
data: {}
{{- end }} 